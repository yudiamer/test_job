stage('Build Docker Image') {
    steps {
        echo 'Building Docker image...'
        sh 'docker --version'
        sh 'ls -l'
        sh 'cat Dockerfile || echo "No Dockerfile found"'
        sh 'docker build -t trade-trends-automation:${BUILD_NUMBER} .'
    }
}